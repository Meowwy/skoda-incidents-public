# Soubor: systems/andon/andon.yaml
# Popis: Otázky pro diagnostiku systému ANDON
# Poznámka: Toto je šablona — doplňte skutečné otázky a možnosti

steps:
  # ------------------------------------------
  # Otázka 1: K čemu se systém používá?
  # ------------------------------------------
  - id: andon_purpose
    type: select
    question: "K čemu se systém ANDON používá?"
    options:
      - "TODO: Doplňte účel 1"
      - "TODO: Doplňte účel 2"
      # - "Něco jiného"  # slepá ulička — odkomentujte pokud chcete
    render_if: "system == 'ANDON'"

  # Slepá ulička (odkomentujte pokud máte volbu "Něco jiného")
  # - id: andon_purpose_dead_end
  #   type: info
  #   question: "Mimo standardní proces"
  #   description: "Kontaktujte svého nadřízeného."
  #   render_if: "system == 'ANDON' && andon_purpose == 'Něco jiného'"
  #   action: stop

  # ------------------------------------------
  # Otázka 2: Jaké zařízení?
  # ------------------------------------------
  - id: andon_device
    type: select
    question: "Jaké zařízení používáte?"
    options:
      - "TODO: Doplňte zařízení 1"
      - "TODO: Doplňte zařízení 2"
    render_if: "system == 'ANDON'"

  # ------------------------------------------
  # Otázka 3: Co přesně nefunguje?
  # ------------------------------------------
  - id: andon_symptom
    type: select
    question: "Co přesně nefunguje?"
    options:
      - "TODO: Doplňte příznak 1"
      - "TODO: Doplňte příznak 2"
      # Pokud potřebujete seskupit příznaky, použijte:
      # - group: "Název skupiny"
      #   options:
      #     - "Příznak A"
      #     - "Příznak B"
    render_if: "system == 'ANDON'"

  # ==========================================
  # DIAGNOSTICKÉ VĚTVE
  # ==========================================
  # Pro každý příznak vytvořte diagnostický soubor a vložte ho sem:
  # - $include: "andon_NAZEV_diagnostic.yaml"
